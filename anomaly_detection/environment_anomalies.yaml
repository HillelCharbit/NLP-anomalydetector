name: anomaly_detection
channels:
  - defaults
dependencies:
  - pip  
  - ipykernel
  - pip:
      - chronos-forecasting
      - numpy
      - matplotlib
      - pandas
      - pyod
      - seaborn
      - yfinance==0.2.37